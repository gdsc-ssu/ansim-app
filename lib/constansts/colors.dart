import 'package:flutter/material.dart';

final class AnsimColor {
  // Brand Color
  static const primary = Color(0xFFFF6B35);

  // Severity
  static const critical = Color(0xFFFF3B30);
  static const warning = Color(0xFFFF9500);
  static const minor = Color(0xFFFFCC00);
  static const resolved = Color(0xFF34C759);

  // Semantic
  static const location = Color(0xFF3B82F6);

  // Natural
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary= Color(0xFF666666);
  static const textHint= Color(0xFF999999);
  static const bgSecondary= Color(0xFFF2F3F5);
  static const border = Color(0xFFE5E5E5);
  static const bgDefault = Color(0xFFFFFFFF);
}
